interface:
  display_name: "Worktree 直接提交到 Main"
  short_description: "将 worktree 改动 fast-forward 合并到 main；同步时推送，领先时仅本地提交"
  default_prompt: "使用 $ccccr-worktree-commit-main 将 worktree 中的改动直接提交到 main。"
