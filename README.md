# dotfiles

我的配置文件仓库。各目录说明:

| 目录 | 内容 |
|------|------|
| `agents/` | AI 工具共享指令与 skills/mcp 清单,见 [agents/README.md](agents/README.md) |
| `shell/` | shell 配置(zsh 等) |
| `startup/` | 启动脚本 |
| `tools/` | 各类工具配置 |

安装:各目录自带说明与脚本(如 `agents/install.sh`),符号链接由对应脚本管理。
